[project]
name = "blog-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mario Velasco", email = "105537753+mariovelascodev@users.noreply.github.com" }
]
requires-python = ">=3.12.10"
dependencies = [
    "aiomysql>=0.3.2",
    "fastapi[standard]>=0.141.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
blog-api = "blog_api:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
